# Robolectric runs the SDK against a real Android framework implementation rather than mocks,
# which is what makes SharedPreferences, NotificationManager and permission behaviour testable.
# API 33 is pinned because it is the first version with POST_NOTIFICATIONS, so the permission
# split the SDK exists to hide is actually exercised.
sdk=33
